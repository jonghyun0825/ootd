# Step 05. Mock 버전 빌드 검증

## 목표
- `npm run build` 로 mock 버전이 오류 없이 정적 빌드되는지 확인한다.
- TypeScript 타입 오류, 빌드 경고를 모두 해결한다.
- 로컬 미리보기(`npm run preview`)로 모바일 폭에서 화면이 정상 동작하는지 확인한다.

## 완료 기준
- [ ] `npm run build` 성공 (exit code 0)
- [ ] `dist/` 폴더에 정적 파일 생성 확인
- [ ] 콘솔 에러 없이 7개 화면 모두 진입 가능
